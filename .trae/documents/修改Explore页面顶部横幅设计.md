## 实现计划

### 问题分析
用户提到了一个图片标签 `tos-cn-i-zaeny81f73/cn/1264912207986416/image/1764824870107_7f4j6eh935a0_png_1096x279`，看起来是阿里云OSS的图片URL。当前Explore页面使用`TianjinImage`组件显示图片，但没有直接支持这种图片标签的显示。

### 实现方案
1. **修改TianjinImage组件**：
   - 检查当前组件是否已经支持图片标签显示
   - 如果需要，扩展组件以支持图片标签属性

2. **为作品添加图片标签**：
   - 在作品数据结构中添加图片标签字段
   - 确保标签能够正确传递给TianjinImage组件

3. **在Explore页面中显示图片标签**：
   - 检查FeaturedWorks和WorksGrid中的图片显示
   - 确保所有图片都能显示相应的标签

4. **优化标签显示样式**：
   - 确保标签显示位置和样式美观
   - 与现有UI设计风格保持一致

### 预期效果
修改后，Explore页面中的作品图片将能够显示用户提到的图片标签，提供更丰富的图片信息展示。