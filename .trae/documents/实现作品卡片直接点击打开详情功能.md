1. **为作品卡片添加点击事件**：在Explore.tsx中，为作品卡片的外层motion.div元素添加onClick事件，直接跳转到作品详情页
2. **统一导航逻辑**：使用现有的navigate(`/explore/${work.id}`)方法，确保与"查看详情"按钮的导航路径一致
3. **处理事件冒泡**：确保内部按钮（如点赞、收藏）的stopPropagation()不会影响整个卡片的点击事件
4. **优化用户体验**：添加cursor: pointer样式，让用户知道卡片是可点击的
5. **保持现有功能不变**："查看详情"按钮的功能保持不变，提供两种进入详情页的方式

**修改位置**：

* Explore.tsx文件中的作品卡片渲染部分（约1017-1172行）

* 为外层motion.div添加onClick事件和cursor样式

**预期效果**：

* 用户点击作品图片、标题或卡片任意区域（除了内部按钮）都能直接进入作品详情页

* 保持良好的用户体验和视觉反馈

* 不破坏现有功能

