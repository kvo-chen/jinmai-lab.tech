{
  "version": 2,
  "build": {
    "command": "pnpm install && pnpm build",
    "outputDirectory": "dist",
    "installCommand": "pnpm install",
    "nodeVersion": "18.x"
  },
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}